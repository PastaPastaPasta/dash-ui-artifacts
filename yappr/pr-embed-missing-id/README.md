# Yappr embed missing-id evidence

Synthetic deterministic rendering of the exact EmbedPageContent error states at 800x500. Before corresponds to staging base `4105c5d` (`Missing post id.` only). After corresponds to PR #407 head `636b56a6`, which adds `role=alert` and an `Open Yappr` recovery link. This fixture isolates the visible behavior; no credentials or personal data are present.
