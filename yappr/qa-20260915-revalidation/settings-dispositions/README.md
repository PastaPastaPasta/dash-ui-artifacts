# Settings claim dispositions

Exact staging `4105c5d1c914f5d0838619da93c3b8d28b4a780e`, live devnet data, seeded identity `AUcoNeieq1VNiyAkQrrn4XE8rpaRT3gSUCQsy7E2SN7x`.

Auth Vault/encryption controls are available. The real UI exposes Enter Key, Add Password Unlock and Add Passkey; Enter Key opens the shown modal. Private Feed > Enable Private Feed opens the shown key input. No key material was entered or published.

The duplicate mobile navigation report is retracted. Viewport captures at top and bottom show exactly one navigation row. A full-page screenshot misleadingly captures the closed More sheet beyond the actual viewport: the sheet occupies viewport y=788..1094 and the opaque navigation at y=787..844 covers the entire visible part. This remains true after scrolling. The full-page artifact is intentionally excluded from the authoritative evidence package.

See `provenance.json` for geometry, hashes, exact fixture and limits. These screenshots validate the unchanged application, not a before/after fix.
