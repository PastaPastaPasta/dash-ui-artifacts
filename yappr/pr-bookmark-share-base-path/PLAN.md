# Bookmark normal QA and QA84 comparison plan

Source before: `733faf53cd893cba861476a4af75b442ed67ca72`, independent production export `http://127.0.0.1:3278/devnet`. Any fix head is recorded after signed commit and build, before after-capture. Persona55 public identity `HVVUwJLhEngLH5LkZx8FgUkcso28xK7tn5gaaHke5WWu`; ordinary UI bookmarks on existing public posts. Initial UI and independent SDK snapshot must both be empty before clear-all is exercised. Fresh contexts;1280×900 and390×844; light theme; en-US browser defaults. Seeded scoped session is explicitly not login evidence.

| Behavior | Before state | After state | Shared fixture | Visible delta/claim |
|---|---|---|---|---|
| Per-bookmark Share post menu | exact733 baseline | exact full committed fix head, if confirmed | same saved public post, actual clipboard copied URL, fresh context | Before copied destination misses /devnet and fails local deployed route; after exact same post renders at /devnet/post/ |
| Menu context | same baseline | same head | same bookmark and viewport | Compatibility view identifies which separate menu was clicked; no injected clipboard text or fabricated link on page |
| Search/sort/clear | baseline normal UI | not a fix comparison | three explicitly recorded temporary saved public posts, initial empty account | Search content/author/no-match/clear; ascending/descending creation order; clear Cancel/confirm; independent fresh readback; cleanup |

Preserve original screenshots, inspect every final image at original resolution, publish immutable artifact links only for inspected selections. Screenshot alone does not prove actual clipboard content; publish machine-readable clipboard/destination assertions beside it. Do not reuse any provisional diagnostic capture as final exact-head evidence.
